require "spec_helper"

describe UscisQuestions do
  it "has a version number" do
    expect(UscisQuestions::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
